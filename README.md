# ImageSpeak-Image-Caption-Generator
ImageSpeak is an AI-powered image caption generator that automatically creates descriptive captions for images. It uses a Vision Transformer (ViT) with a Vision Encoder-Decoder architecture, BERT tokenizer, and GloVe embeddings. Built with Flutter, Flask, and GCP, it was trained on Flickr30k and MS COCO datasets.
